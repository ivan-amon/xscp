[package]
name = "client"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
xscp = { path = ".." }
tokio = { version = "1", features = ["net", "io-util", "io-std", "macros", "rt-multi-thread"] }
