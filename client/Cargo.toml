[package]
name = "client"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tokio = { version = "1", features = ["net", "io-util", "io-std", "macros", "rt-multi-thread"] }
xscp = { path = ".." }
io = { path = "../io" }
