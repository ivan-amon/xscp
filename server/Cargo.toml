[package]
name = "server"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tokio = { version = "1.52.3", default-features = false, features = ["rt", "net", "io-util", "macros", "rt-multi-thread", "sync"] }
xscp = { path = ".." }
io = { path = "../io" }
